import PalindromeC

final class DosLoader {
    private let folder: String
    private let file: String

    init(foler: String, file: String) {
        self.folder = foler
        self.file = file
    }
}
